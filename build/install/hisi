#!/bin/sh

CURDIR=`pwd`
TARGETDIR=${CURDIR}/build/target/hisi

rm -fr ${TARGETDIR}
mkdir -p ${TARGETDIR}

cp -frvL build/platform/linux/libhilda.so ${TARGETDIR}
cp -frvL build/platform/linux/libhilda.a ${TARGETDIR}
